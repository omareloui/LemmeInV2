{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "start": "denon prod",
    "dev": "denon start",
    "build": "rimraf -rf dist && mkdir dist && denon build",
    "lint": "denon lint"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "rimraf": "^3.0.2",
    "tsconfig": "workspace:*"
  }
}
